# Airflow orchestration showcase for the Flight Disruption Platform.
# Pinned to the Airflow 2.x stable line. Install with the official constraints
# file so the heavy transitive deps resolve cleanly, e.g.:
#
#   PYV=3.12
#   AF=2.10.4
#   pip install -r requirements.txt \
#     --constraint "https://raw.githubusercontent.com/apache/airflow/constraints-${AF}/constraints-${PYV}.txt"
#
# The DAGs only use core operators (Bash/Python/Empty/Branch) + dynamic mapping,
# so no provider packages are strictly required. boto3 is for publish_artifacts.

apache-airflow==2.10.4
boto3>=1.34            # used by scripts/publish_artifacts.py (S3/MinIO upload)

# NOTE: the orchestrated components (flight_ingest, flight_lakehouse, flight_ml,
# dbt-duckdb, flight_contracts) are NOT listed here. They are installed from
# their own sub-projects / pyproject.toml and must be importable on the same
# environment / PYTHONPATH the workers run in. See README.md.
