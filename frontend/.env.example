# Base URL of the FastAPI backend (serving/). No trailing slash.
PUBLIC_API_BASE=http://localhost:8005

# Set to "true" to serve bundled mock data instead of hitting the API.
# Handy for `npm run dev` / a self-contained demo with no backend running.
PUBLIC_USE_MOCK=false
